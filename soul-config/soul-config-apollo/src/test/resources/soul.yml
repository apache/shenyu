#配置的读取模式. local彩用的是yml方式，disconf， 表示可以从选程读取配置信息.
soul:
  server:
    configMode: apollo
    cluster: local
    serverId: local001
    host: host
    monitorPort: 8002
    port: 8003
  apollo:
    appId: soul-test
    metaServer: http://121.43.171.129:8080