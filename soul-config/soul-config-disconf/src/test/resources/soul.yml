#配置的读取模式. local彩用的是yml方式，disconf， 表示可以从选程读取配置信息.
soul:
  server:
    configMode: disconf
    cluster: local
    serverId: local001
    host: host
    monitorPort: 8002
    port: 8003
  disconf:
    enableConf: true
    configServerHost: 192.168.1.82:8081
    version: 2.0.0
    app: soul
    env: rd